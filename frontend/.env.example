# Frontend environment example
# Copy to .env.local and adjust for your environment.
# Base URL of the NexusAgent backend API (no trailing slash).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
